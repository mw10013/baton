PORT=3800
# Only needed by the admin Durable Object explorer (/admin/shop-agent-objects).
CLOUDFLARE_WORKERS_API_TOKEN=
# Only needed when BILLING_ENABLED is "true".
SHOPIFY_PARTNER_API_TOKEN=
# openssl rand -base64 32
ADMIN_AUTH_SECRET=
# Both are required: AdminAuth accepts either, so one can be rotated without
# locking operators out mid-rotation.
ADMIN_PASSWORD=
ADMIN_PASSWORD1=
